// app/voice-chat.tsx
// Phase 4에서 구현 예정 (ZegoCloud SDK는 개발 빌드에서만 동작)
import { Text, View } from 'react-native';

export default function VoiceChatScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>음성 통화 화면 (준비 중)</Text>
    </View>
  );
}